# Services

Use `.claude/skills/generated/services/SKILL.md` first for this subtree.

This folder owns persistence, AI providers, search indexing, storage, and related business logic. Treat the GitNexus graph as the source of truth for caller/callee relationships here.
