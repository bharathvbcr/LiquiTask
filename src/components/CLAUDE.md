# UI Components

Use `.claude/skills/generated/components/SKILL.md` first for this subtree.

This is the primary component implementation surface. When a UI change crosses into services or hooks, use GitNexus to trace the full flow before editing.
