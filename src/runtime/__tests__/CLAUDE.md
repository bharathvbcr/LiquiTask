# Runtime Tests

Use `.claude/skills/generated/runtime/SKILL.md` first for this subtree.

This folder covers runtime helpers and their tests. Keep test updates aligned with the runtime map instead of treating them as isolated cases.
