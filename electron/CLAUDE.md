# Electron

Use `.claude/skills/generated/electron/SKILL.md` first for this subtree.

This folder owns the desktop bridge, preload, and main-process wiring. Keep Electron changes narrow and verify the renderer contract against the GitNexus map before touching IPC shapes.
