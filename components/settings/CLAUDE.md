# Settings

Use `.claude/skills/generated/components/SKILL.md` and `.claude/skills/generated/settings/SKILL.md` first for this subtree.

This area contains settings panels and related UI wiring. Prefer GitNexus to trace which settings panel calls into services or storage before editing.
