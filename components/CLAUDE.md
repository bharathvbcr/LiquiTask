# Components

Use `.claude/skills/generated/components/SKILL.md` first for this tree.

This folder contains the legacy/top-level component surface that still matters for agent navigation. When a symbol appears in both `components/` and `src/components/`, use the GitNexus skill and the actual import graph to decide which file is authoritative.
